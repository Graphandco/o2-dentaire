services:
   app:
      container_name: o2-dentaire-dev
      build:
         context: .
         dockerfile: Dockerfile.dev
      restart: unless-stopped
      ports:
         - "3005:3005"
      env_file:
         - .env
      volumes:
         - .:/app
         - /app/node_modules
         - /app/.next
      environment:
         NODE_ENV: development
         PORT: 3005
